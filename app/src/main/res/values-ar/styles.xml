<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Note: I have commented where I edited only. Any attribute not mentioned in the comment is not edited -->

    <style name="text_mainActivity_quizQuestion">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingRight">10dp</item> <!-- changed paddingLeft to paddingRight -->
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="RadioGroup_mainActivity_verticalIndentation">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:orientation">vertical</item>
        <item name="android:paddingTop">8dp</item>
        <item name="android:paddingRight">24dp</item> <!-- changed paddingLeft to paddingRight -->
        <item name="android:textDirection">rtl</item>
    </style>

    <style name="RadioButton_mainActivity_textIndentation_8dp">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingRight">8dp</item> <!-- changed paddingLeft to paddingRight -->
    </style>

    <!-- changed layout_marginLeft to layout_marginRight and paddingLeft to paddingRight. Added textDirection -->
    <style name="CheckBox_mainActivity_textIndentation">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginRight">24dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:textDirection">rtl</item>
    </style>

    <!-- Edit: changed layout_marginLeft to layout_marginRight and paddingLeft to paddingRight. Added textDirection attribute -->
    <style name="EditBox_mainActivity_indentation">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginRight">24dp</item>
        <item name="android:paddingRight">8dp</item>
        <item name="android:textDirection">rtl</item>
    </style>
</resources>
